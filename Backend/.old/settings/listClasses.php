<?php

$initialBaseUrl = './classes/';
$endBaseUrl = '.class.php';

include_once $initialBaseUrl . 'empleado' . $endBaseUrl;
include_once $initialBaseUrl . 'empleador' . $endBaseUrl;
include_once $initialBaseUrl . 'empresa' . $endBaseUrl;
include_once $initialBaseUrl . 'estudios' . $endBaseUrl;
include_once $initialBaseUrl . 'experiencia' . $endBaseUrl;
include_once $initialBaseUrl . 'idioma' . $endBaseUrl;
include_once $initialBaseUrl . 'postulados' . $endBaseUrl;
include_once $initialBaseUrl . 'vacante' . $endBaseUrl;
include_once $initialBaseUrl . 'session' . $endBaseUrl;
include_once $initialBaseUrl . 'session_empleador' . $endBaseUrl;
